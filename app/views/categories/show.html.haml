%h3 Category
%p
  Name: 
  = @category.name if @category.name 

  = link_to "Edit record", edit_category_path(@category), class: 'button-xs'
  = link_to "Delete record", category_path(@category), :data => { confirm: "Are you sure?" }, method: :delete, class: 'button-xs'
  = link_to "Show all", categories_path(), class: 'button-xs'

